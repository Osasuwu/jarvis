"""Retired: pinned the `pytest-db-task-queue` job wiring, which pinned
tests/reactive_core/test_task_queue_issue_number_db.py.

Both the job and the test file were demolished with reactive-core in #1802.
This file intentionally left as an empty module — auto-mode's file-deletion
guard blocked `git rm` for this path; ask the operator to delete it outright.
"""
